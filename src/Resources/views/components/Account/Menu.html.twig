{% props
    activeRoute = null,
    cva = {},
%}

{% do vi_define_cva(cva) %}

{% do vi_define_attrs(['footer', 'register']) %}

<div
    {{ attributes.defaults(context.customer ? {} : {
        role: 'dialog',
        'aria-label': 'account.myAccount'|trans|striptags,
    }) }}
    class="{{ vi_class('root') }}"
>
    {% if context.customer %}
        {% block user_actions %}
            <twig:ViewsTheme:Account:Actions 
                nav:class="py-2"
                :activeRoute="activeRoute" />
        {% endblock %}
    {% else %}
        {% block login %}
            <twig:ViewsTheme:Account:Login 
                class="p-4 border-bottom"
                :description="false"
                :username:label="false"
                :password:label="false" />
        {% endblock %}

        {% block footer %}
            <div
                {{ vi_attrs('footer') }}
                class="{{ vi_class('footer') }}"
            >
                <twig:ViewsTheme:Button
                    class="{{ vi_class('register') }}"
                    {{ ...vi_attrs('register').defaults({
                        type: 'link',
                        color: 'outline-primary',
                        href: path('frontend.account.login.page'),
                        label: 'account.orRegisterLink'|trans|striptags,
                    }).all() }}
                />
            </div>
        {% endblock %}
    {% endif %}
</div>
