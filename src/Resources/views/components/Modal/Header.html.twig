{% props
    title = null,
    cva = {},
%}

{% do vi_define_cva(cva) %}
{% do vi_define_attrs(['title', 'close']) %}

<div
    {{ attributes }}
    class="{{ vi_class('root') }}"
>
    {% block content %}
        {% block title %}
            <h1
                {{ vi_attrs('title') }}
                class="{{ vi_class('title') }}"
            >
                {% if title %}
                    {{ title }}
                {% endif %}
            </h1>
        {% endblock %}

        {% block close %}
            <twig:ViewsTheme:Modal:Close
                {{ ...vi_attrs('close').defaults({}).all() }}
            />
        {% endblock %}
    {% endblock %}
</div>
