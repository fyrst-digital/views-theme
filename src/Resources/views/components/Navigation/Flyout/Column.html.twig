{% props
    item,
    level = 0,
    maxDepth = 3,
    cva = {},
%}

{% set category = item.category %}
{% set name = category.translated.name %}
{% set isFolder = category.type == 'folder' %}
{% set children = item.children %}
{% do vi_define_cva(cva, ['root', 'heading', 'list', 'item']) %}
{% do vi_define_attrs(['heading', 'list']) %}

<div
    {{ attributes }}
    class="{{ vi_class('root') }}"
>
    {% if isFolder %}
        <div
            {{ vi_attrs('heading') }}
            class="{{ vi_class('heading') }}"
            title="{{ name }}"
        >
            {{ name }}
        </div>
    {% else %}
        <a
            {{ vi_attrs('heading').defaults({
                href: category_url(category),
                title: name,
                target: category_linknewtab(category) ? '_blank' : null,
            }) }}
            class="{{ vi_class('heading') }}"
        >
            {{ name }}
        </a>
    {% endif %}

    {% if children|length > 0 and level < maxDepth %}
        <ul
            {{ vi_attrs('list') }}
            class="{{ vi_class('list') }}"
        >
            {% for child in children %}
                {% set childCat = child.category %}
                {% set childHasChildren = child.children|length > 0 %}
                {% set childSkipped = childCat.type == 'folder' and not childHasChildren %}

                {% if not childSkipped %}
                    <twig:ViewsTheme:Navigation:Flyout:Item
                        class="{{ vi_class('item') }}"
                        :item="child"
                        :level="level + 1"
                        :maxDepth="maxDepth"
                    />
                {% endif %}
            {% endfor %}
        </ul>
    {% endif %}
</div>
