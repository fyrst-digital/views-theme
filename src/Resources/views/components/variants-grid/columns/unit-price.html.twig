{%
  set classes = vi_define_classes({
    cell: ['variants-grid-column-price'],
  }, classes|default({}), replaceClasses|default(false))
%}

{% block component_variants_grid_column_unit_price_cell %}
  <td {{ classes.cell | vi_attr_classes }}>
    {{ variant.calculatedPrice.unitPrice|currency }}
  </td>
{% endblock %}
