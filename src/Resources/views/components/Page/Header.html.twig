{% props
    cva = {},
%}

{% set cx = vi_cva({
    root: { base: '' },
}|replace_recursive(cva)) %}

<header 
    class="{{ cx.root.apply() }}">
    {% block content %}

        {% block logo %}
            <twig:ViewsTheme:Page:Logo />
        {% endblock %}
    {% endblock %}
</header>
