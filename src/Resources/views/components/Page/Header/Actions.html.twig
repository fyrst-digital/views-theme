{% props
    wishlist = config('core.cart.wishlistEnabled'),
    cva = {},
%}

{% set cx = vi_cva({
    root: { base: 'col d-flex justify-content-end align-items-center' },
}|replace_recursive(cva)) %}

<div
    {{ attributes.without('class').without('data-component-template') }}
    class="{{ cx.root.apply() }}"
>
    {% block content %}

        {% block menu %}
            <twig:ViewsTheme:Navigation:Drawer:Action
                class="header-action" />
        {% endblock %}

        {% block search %}
            <twig:ViewsTheme:Search:Action 
                class="header-action" />
        {% endblock %}

        {% block wishlist %}
            {% if wishlist %}
                <twig:ViewsTheme:Wishlist:Action
                    class="header-action position-relative" />
            {% endif %}
        {% endblock %}  

        {% block account %}
            <twig:ViewsTheme:Account:Action 
                class="mt-2 p-0 vi-account__dropdown"
                toggle:class="header-action" />
        {% endblock %}

        {% block cart %}
            <twig:ViewsTheme:Page:Header:Action:Cart
                class="{{ actionClass }}" />
        {% endblock %}

    {% endblock %}

</div>
