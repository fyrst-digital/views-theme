{% props
    searchTerm = null,
    minSearchLength = null,
    cva = {},
%}

{% set cx = vi_cva_from_file(cva) %}

{% set attrs = {
    panel: attributes.nested('panel'),
} %}

<div
    {{ attributes.defaults({
        id: 'vi-search-overlay',
        role: 'dialog',
        'aria-modal': 'true',
        'aria-label': 'header.searchButton'|trans|striptags,
        'aria-hidden': 'true',
        inert: true,
        'data-component': 'ViewsTheme:Search:Overlay',
    }) }}
    class="{{ cx.root.apply() }}"
>
    {% block backdrop %}
        <twig:ViewsTheme:Backdrop componentName="ViewsTheme:Search:Overlay" />
    {% endblock %}

    {% block close %}
        <twig:ViewsTheme:Search:Overlay:Close />
    {% endblock %}

    {% block panel %}
        <div
            {{ attrs.panel }}
            class="{{ cx.panel.apply() }}"
        >
            {% block bar %}
                <twig:ViewsTheme:Search:Bar
                    :searchTerm="searchTerm"
                    :minSearchLength="minSearchLength"
                    input:class="fs-lg-4 fw-semibold"
                />
            {% endblock %}
        </div>
    {% endblock %}
</div>
