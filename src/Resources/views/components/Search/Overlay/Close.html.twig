{% props
    cva = {},
%}

{% do vi_define_cva(cva) %}

<button
    {{ attributes.defaults({
        type: 'button',
        'data-component': 'ViewsTheme:Search:Overlay:Close',
        'aria-label': 'header.searchCloseButton'|trans|striptags,
    }) }}
    class="{{ vi_class('root') }}"
>
    {% block icon %}
        {{ vi_icon('x') }}
    {% endblock %}
</button>
