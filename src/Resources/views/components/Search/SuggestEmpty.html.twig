{% props
    cva = {},
%}

{% set cx = vi_cva_from_file(cva) %}

<div
    {{ attributes }}
    class="{{ cx.root.apply() }}"
>
    {% block content %}
        {{ 'header.searchNoResult'|trans|sw_sanitize }}
    {% endblock %}
</div>
