{% props
    cva = {},
%}

{% do vi_define_cva(cva) %}

<div
    {{ attributes }}
    class="{{ vi_class('root') }}"
>
    {% block content %}
        {{ 'header.searchNoResult'|trans|sw_sanitize }}
    {% endblock %}
</div>
