{% props
    message = 'checkout.cartEmpty'|trans|sw_sanitize,
    cva = {},
%}

{% do vi_define_cva(cva) %}

<div
    {{ attributes.defaults({
        role: 'status',
    }) }}
    class="{{ vi_class('root') }}"
>
    {% block content %}
        {{ message }}
    {% endblock %}
</div>
