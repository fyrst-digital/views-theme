{% props
    href = path('frontend.checkout.cart.page'),
    label = 'viewsTheme.cart.cart'|trans|sw_sanitize,
    title = 'viewsTheme.cart.cart'|trans|striptags,
    color = 'light',
    cva = {},
%}

{% set cx = vi_cva_from_file(cva) %}

{% block content %}
    <twig:ViewsTheme:Button
        class="{{ cx.root.apply() }}"
        {{ ...attributes.defaults({
            type: 'link',
            href: href,
            label: label,
            title: title,
            color: color,
        }).all() }}
    />
{% endblock %}
