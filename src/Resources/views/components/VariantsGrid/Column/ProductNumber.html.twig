{% props
    variant,
    cva = {},
%}

{% do vi_define_cva({
    root: { base: 'vi-variants-grid-column-number variants-grid-column-number' },
}|replace_recursive(cva)) %}

<td {{ attributes }} class="{{ vi_class('root') }}">
    {{ variant.productNumber }}
</td>
