{% props
    cva = {},
%}

{% do vi_define_cva({
    root: { base: 'vi-variants-grid-alert alert alert-danger d-none' },
}|replace_recursive(cva)) %}

<div
    {{ attributes.defaults({
        role: 'alert',
    }) }}
    class="{{ vi_class('root') }}"
></div>
