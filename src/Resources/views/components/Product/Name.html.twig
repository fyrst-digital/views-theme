{% props
    product,
    showLink = true,
    url = null,
    name = null,
    tag = 'div',
    cva = {},
%}

{% set productName = name is not null ? name : product.translated.name %}
{% set productUrl = url is not null ? url : seoUrl('frontend.detail.page', { productId: product.id }) %}
{% set cx = vi_cva({
    root: { base: 'vi-product-name' },
    link: { base: 'vi-product-name__link' },
}|replace_recursive(cva)) %}

<{{ tag|raw }}
    {{ attributes }}
    class="{{ cx.root.apply() }}"
>
    {% if showLink is same as(true) %}
        <a
            href="{{ productUrl }}"
            title="{{ productName }}"
            {{ attributes.nested('link') }}
            class="{{ cx.link.apply() }}"
        >
            {{ productName }}
        </a>
    {% else %}
        {{ productName }}
    {% endif %}
</{{ tag|raw }}>
