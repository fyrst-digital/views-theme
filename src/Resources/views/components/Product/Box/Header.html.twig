{% set cx = vi_cva_from_file(cva) %}

{% set attrs = {
    rating: attributes.nested('rating'),
    name: attributes.nested('name'),
} %}

<div
    {{ attributes }}
    class="{{ cx.root.apply() }}"
>
    {% block rating %}
        {% if showRatingBlock %}
            <twig:ViewsTheme:Review:Rating
                class="{{ cx.rating.apply() }}"
                {{ ...attrs.rating.defaults({
                    points: product.ratingAverage,
                }).all() }}
            />
        {% endif %}
    {% endblock %}

    {% block name %}
        <twig:ViewsTheme:Product:Name
            class="{{ cx.name.apply() }}"
            {{ ...attrs.name.defaults({
                product: product,
                url: href,
            }).all() }}
        />
    {% endblock %}
</div>
