{% props
    label = 'listing.boxLabelNew'|trans|sw_sanitize,
    cva = {},
%}

{% do vi_define_cva(cva) %}

{% block content %}
    <twig:ViewsTheme:Badge
        class="{{ vi_class('root') }}"
        {{ ...attributes.defaults({
            type: 'new',
            label: label,
        }).all() }}
    />
{% endblock %}
