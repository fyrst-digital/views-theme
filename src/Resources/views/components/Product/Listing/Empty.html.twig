{% props
    message = 'listing.emptyResultMessage'|trans|sw_sanitize,
    cva = {},
%}

{% do vi_define_cva(cva) %}

<div
    {{ attributes.defaults({
        role: 'status',
    }) }}
    class="{{ vi_class('root') }}"
>
    {% block content %}
        <twig:ViewsTheme:Alert
            type="info"
            content="{{ message }}"
            announceOnLoad
        />
    {% endblock %}
</div>
