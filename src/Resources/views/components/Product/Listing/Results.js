/**
 * Mount identity for Product:Listing:Results island (swap target).
 * Behaviour lives on Product:Listing owner + Pagination/Sorting.
 */
export default class ProductListingResults extends ShopwareComponent {
    init() {}
}
