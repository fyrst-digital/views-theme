{% props
    cva = {},
%}

{% do vi_define_cva(cva) %}

<button
    {{ attributes.defaults({
        type: 'button',
        'data-component': 'ViewsTheme:Drawer:Close',
        'aria-label': 'general.offcanvasCloseMenu'|trans|striptags,
        title: 'general.offcanvasCloseMenu'|trans|striptags,
    }) }}
    class="{{ vi_class('root') }}"
>
    {% block icon %}
        {{ vi_icon('x') }}
    {% endblock %}
</button>
