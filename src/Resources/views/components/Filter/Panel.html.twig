{% sw_extends '@Storefront/storefront/component/listing/filter-panel.html.twig' %}

{% props
    cva = {},
%}

{% set cx = vi_cva({
    header: { base: 'vi-filter-panel__header' },
    title: { base: 'vi-filter-panel__title' },
    close: { base: 'vi-filter-panel__close btn js-offcanvas-close' },
    container: { base: 'vi-filter-panel__container' },
}|replace_recursive(cva)) %}

{% set attrs = {
    header: attributes.nested('header'),
    title: attributes.nested('title'),
    close: attributes.nested('close'),
    container: attributes.nested('container'),
} %}

{% block component_filter_panel_header %}
    <div {{ attrs.header }} class="{{ cx.header.apply() }}">
        <div {{ attrs.title }} class="{{ cx.title.apply() }}">
            {{ 'listing.filterTitleText'|trans }}
        </div>
        <div {{ attrs.close }} class="{{ cx.close.apply() }}">
            {{ vi_icon('x') }}
        </div>
    </div>
{% endblock %}

{% block component_filter_panel_items_container %}
    <div {{ attrs.container }} class="{{ cx.container.apply() }}">
        {% block component_filter_panel_items %}
            {{ parent() }}
        {% endblock %}
    </div>
{% endblock %}
