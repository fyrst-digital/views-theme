/**
 * Empty / not-found identity for Address:Manager:List.
 *
 * @extends {ShopwareComponent}
 */
export default class AddressManagerStatus extends ShopwareComponent {
    static options = {
        kind: 'empty',
    }

    init() {}
}
