{% props
    lineItem,
    deliveries = null,
    cva = {},
%}

{% set options = lineItem.payload.options ?? [] %}
{% do vi_define_cva(cva) %}

{% do vi_define_attrs(['variations', 'features', 'deliveryDate']) %}

<div
    {{ attributes }}
    class="{{ vi_class('root') }}"
>
    {% block variations %}
        {% if options is not empty %}
            <twig:ViewsTheme:Product:Variations
                class="d-flex flex-wrap align-items-center gap-1 text-body-secondary small"
                {{ ...vi_attrs('variations').defaults({
                    variations: options,
                }).all() }}
            />
        {% endif %}
    {% endblock %}

    {% block features %}
        <twig:ViewsTheme:LineItem:Features
            {{ ...vi_attrs('features').defaults({
                lineItem: lineItem,
            }).all() }}
        />
    {% endblock %}

    {% block deliveryDate %}
        {% if config('core.cart.showDeliveryTime') %}
            <twig:ViewsTheme:LineItem:DeliveryDate
                {{ ...vi_attrs('deliveryDate').defaults({
                    lineItem: lineItem,
                    deliveries: deliveries,
                }).all() }}
            />
        {% endif %}
    {% endblock %}
</div>
